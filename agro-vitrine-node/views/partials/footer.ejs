</main>
<footer class="border-t bg-white text-sm text-gray-500 px-4 py-6 text-center">
  © <%= new Date().getFullYear() %> Marcos. Todos os direitos reservados.
</footer>
<script>
  // Rolagem suave ao clicar nos links de âncora
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        window.scrollTo({
          top: target.offsetTop - 80, // ajuste conforme altura do header
          behavior: 'smooth'
        });
      }
    });
  });
</script>
</body>
</html>
